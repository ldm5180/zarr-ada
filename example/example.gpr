with "../zarr.gpr";
project Example is

   for Source_Dirs use (".");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   for Main use ("read_spxw.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-O2", "-g", "-gnat2022", "-gnatwa");
   end Compiler;

end Example;
