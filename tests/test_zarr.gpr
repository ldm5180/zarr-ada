with "aunit";
with "../zarr.gpr";

--  Test runner project for zarr, built and executed by `alr test`
--  (see the [[actions]] entry in alire.toml).  It withs the library under
--  test plus the AUnit framework that Alire provides as a dependency.
project Test_Zarr is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";
   for Main use ("test_runner.adb");

   package Compiler is
      for Default_Switches ("Ada") use
        ("-g", "-gnata", "-gnatwa", "-gnatwe", "-gnat2022");
      --  -gnata keeps the library's pre/postconditions live during testing;
      --  -gnatwe holds the test code itself to warnings-as-errors.
   end Compiler;

   package Binder is
      for Switches ("Ada") use ("-Es");  --  Symbolic traceback.
   end Binder;

end Test_Zarr;
